# 三角狐 y7c_0.2

江汉大学个人课表应用，Android 8.0 及以上。 所有内容源自https://github.com/shiyuyu0w0/sanjiaohu_JHUN 请支持原始开发者!!!

## y7c_0.2：版本号提升

- 仅提升版本号：对外 versionName y7c_0.2，内部 versionCode 递增至 33。功能与 y7c_0.1 相同，无代码逻辑变更。
- versionCode 必须递增：沿用 32 会被 Android 拒绝覆盖安装。
- 签名说明：本机 build-local.ps1 的密钥生成在 -Work 目录内。复用同一 -Work 目录构建，则本包与之前的本机 y7c_0.1 包同密钥、可互相覆盖；但都不能覆盖 1.0.1 公共发布版（不同密钥），强行安装需先卸载并会清除课表、成绩、自定义课程、背景与登录凭证。

## y7c_0.1：共享 UI 工具类与深色模式开关

- 新增 Ui.java，承载 7 个无状态视图工具方法（dp / column / row / weighted / space / place / shape）。改动前这些实现被逐份复制在 7 个类里：dp 重复 7 份、shape 5 份、column 4 份、row 3 份。
- 调用类保留同名方法并在内部委托给 Ui。MainActivity 被另外 5 个类（CalibrationSheet、CourseEditor、ThemeColorSheet、CourseDetailSheet、UiSheet）持有并直接调用 a.dp() / a.shape()，保留同名方法后这些调用点无需改动。
- 刻意逐字保留两处原有数值语义：LoginActivity.shape(int) 仍是固定圆角 16 的单参数形式（若误统一为双参数形式，登录页输入框与「登录」按钮会静默变成直角）；Ui.dp 仍是 density*value + 0.5f 后截断（不能改成 Math.round，其对负数舍入方向相反）。
- 对外版本号 y7c_0.1，内部 versionCode 递增至 32（深色模式开关与 Ui 提取同属这一个当时尚未发布的版本，故未再次递增）。
- 新增深色模式开关：「更多 → 外观 → 深色模式」，一个真实 Switch，选择保存在 settings 的 darkMode 布尔值里，重启后仍生效。
- 引入 12 个语义颜色角色（sheetSurface / controlSurface / focusSurface / gridSurface / selectedSurface / outline / divider / rippleMask / wallpaperScrim / controlThumb / disabledTrack / error），13 个调用点改为读取角色而不再内联「向白混合」的浅色字面量。这是深色模式能成立的前提：任何一处漏改都会在深色下留下浅色块。
- 新增 AppTheme.java 作为 Android 侧桥接：读取开关、生成配色，并统一设置状态栏、导航栏和窗口底色（含图标明暗极性）。窗口底色必须在代码里设——手动开关不能用 values-night 资源，否则系统深色与用户手动选择会互相矛盾。
- 浅色取值刻意沿用现有数字，因此引入语义角色本身不改变界面。两处需要说明：divider 与 outline 分开，因为原代码对「细分隔线（约 0.88）」和「真实边框（约 0.55）」本就用了两个不同数值，合并会让所有列表分隔线明显变深；原先服务于同一角色的多个近似值（0.86 / 0.88 / 0.90 / 0.94）统一取 0.90，四者同处近白色带，肉眼不可辨，但这确实是一次数值变化，故在此记录而非默不作声。
- 主题无关的颜色刻意保留内联：色轮内部、赞助码的白色底板（二维码需要浅底才能扫描）、PorterDuff 剪裁遮罩、WallpaperStore 的图片铺白。
- 准确表述：本版消除了重复实现并逐字保留原有数值语义；但**深色模式从未在真机上目视确认过**，仓库内的测试也不含任何布局像素断言，浅色外观「未重绘」是由数值推导而非截图像素比对得出。深色配色的扩展断言（2014 组）只在开发机上运行过，按「仓库仅收录应用代码」的约定未纳入提交。

## v1.0.1：首个公共发布版本

- 底部导航与页面标题“用户”更名为“个人”；个人页右上角新增详情图标，打开“关于”页面。
- 关于页面使用跟随当前主题的圆角卡片，显示“赞助”、原始赞助码、“觉得好用就打赏一杯咖啡吧”，以及当前安装版本 v1.0.1。赞助码内置保存，离线可查看。
- 对外版本号调整为 1.0.1，内部 versionCode 递增至 31；沿用包名与签名，支持覆盖此前测试版本。

以下为此前测试版本的开发记录。

## 1.6.7：兼容无独立加密字段的学校登录表单

用户确认 1.6.6 的学校页面验证已能登录，原生表单仍被“缺少密码加密字段”检查阻止。这个检查把之前桌面页面的 passwordEncrypt 字段错误地当成了所有学校模板的必要条件。

- 不再要求 HTTP 登录页存在 passwordEncrypt；填写可见账号/密码字段后调用学校自己的登录按钮，允许学校模板在原密码字段内或提交过程中自行处理密码。学校令牌、隐藏字段和处理算法不由 App 替换。
- 若表单实际具有独立 passwordEncrypt 字段，仍保留该字段的提交检查。学校校验阻止提交、未填写验证码、非 POST 或站外提交地址仍会停止原生提交。
- 修正提交触发页面跳转之后的回调竞态：过期提交回调不再把 submitted 状态重置为 false，避免干扰后续认证成功时的凭证保存。凭证仍须在确认认证成功后写入独立加密存储。

验证：156 项合成表单检查包含缺少独立加密字段、学校处理原密码字段、验证码和提交目标拦截；既有页面探测、登录状态与教务脚本回归通过。学校网页登录成功由用户反馈确认；本次原生登录和凭证保存仍待手机验证。

## 1.6.6：遵循学校真实 HTTP 认证回跳

手机 1.6.5 的实际记录显示：HTTPS 登录请求收到服务器返回的 HTTP 重定向，App 改写回 HTTPS，随后同一 HTTP 重定向再次出现。全过程未显示登录表单，后续脚本因此运行在空白或错误页面上。强制升级与学校实际认证链路冲突，是这份记录中已经确认的阻断因素。

- 移除认证 HTTP 重定向的强制升级和相关重复升级分支；由 WebView 正常继续学校的跳转，保留请求、会话和回跳参数。初始地址仍为 HTTPS，若学校返回 HTTP，则按其真实协议继续。
- Android 网络配置仅新增 authserver.jhun.edu.cn 这个确切域名的 HTTP 许可。全局明文限制保留，其他域名未开放，证书错误仍取消。
- 登录页面及表单目标接受该认证域名的默认 HTTP/HTTPS 端口，保留学校原始 POST action；继续检查登录路径、回跳白名单、非 GET 提交和学校 passwordEncrypt 字段。HTTP 表单缺少加密字段或提交时没有有效加密结果则阻止原生提交。
- 保留加密凭证存储、其他账号隔离、移动报修回跳和连接诊断。诊断 DOCUMENT=2 表示已显示 HTTP 认证表单，1 表示 HTTPS 表单，0 表示尚非有效登录表单。

传输限制：HTTP 页面没有 HTTPS 的传输保护，学校的页面密码加密不等同于 HTTPS。此次兼容遵循用户指定的学校 HTTP 登录链路，不应描述为全程 HTTPS 登录。

验证：135 项模拟登录检查、47 项 URL 策略、59 项导航流程、31 项诊断隐私、20 项文档探测和既有教务脚本检查通过。导航测试覆盖实际 HTTPS→HTTP→后勤 CAS→移动表单序列，配置检查确认 HTTP 仅限三个精确学校域名。仍待手机端登录成功验收。

## 1.6.5：内置浏览器兼容、页面时序与手机诊断

用户确认同一手机及网络下普通浏览器能够正常登录，但 App 仍报告重复跳转和当前页面不是 HTTPS 登录页。这说明之前的 URL 修改没有完成手机端问题的修复。

- 统一认证专用 WebView 从系统默认 UA 中去除 ; wv 与 Version/4.0 标记，保留系统实际 Chromium 版本及移动端信息。用于兼容可能对内置浏览器采取不同跳转的学校页面；UA 是否是本次根因尚未得到真机验证。其余教务 WebView 不受影响。
- 页面开始加载、重新加载及失败时立即撤销表单可用状态。新页面提交可见或完成加载后才允许执行登录适配。脚本回调携带本次页面与登录尝试编号，页面切换后的旧回调不再覆盖新的登录状态。
- 原生表单和失败页增加主题色“连接诊断”入口，显示实际 App/WebView 版本、最近 40 条跳转和错误码、实际 DOM 来源分类及登录表单是否存在。用户点击后可复制；不自动上传，也不落盘。
- 诊断只保留白名单站点和路径，去除全部 URL 查询、fragment 和 jsessionid。未知路径、来源被替换为固定分类，不记录账号、密码、Cookie、票据、页面正文或表单值。

验证：31 项 UA/诊断隐私检查、20 项页面状态探测检查、125 项登录表单检查、47 项 URL 策略、64 项导航与既有教务脚本回归通过。手机端问题仍需要安装后的行为或诊断记录验证；本版不能声称已经完成真实认证登录验收。

## 1.6.4：直连后勤 CAS 认证

- 根据用户提供的地址，将统一认证和报修入口改为 authserver 的 HTTPS 登录页，service 直接指向 http://hqfw.jhun.edu.cn/wsbx/login/cas#/wybx。移除入口中的服务大厅 login/appShow 中转。#/wybx 编码在 service 参数内部，不能成为认证页面自身的 fragment。
- Edge 实际观察：该地址显示学校统一认证表单，随后回跳到 http://hqfw.jhun.edu.cn/wsbx/#/wybx，页面标题为 403。App 在收到这个明确的目录回跳时打开用户指定的移动版 HTML 入口，保留 CAS 刚建立的会话；不扩大到其他 403 页面。
- 登录成功判断不再接受任意后勤域名页面；CAS 回调路径或目录不能被当作报修页面。观察到认证票据之后，还要抵达报修 HTML 页面才处理成功。

验证：125 项合成表单检查、47 项 URL 策略检查、64 项导航检查，以及已有教务脚本和查询解析检查。Edge 中这次后续打开移动版被客户端 ERR_BLOCKED_BY_CLIENT 拦截，尚未验证本次直连的移动表单最终成功；没有 Android 真机端到端验收。代码中的回跳修正不能代替真实登录验证。

## 1.6.3：修正认证回跳误判

用户截图显示应用主动报出 HTTP/HTTPS 循环和表单不支持，而非学校返回密码错误。旧导航保护的 upgradeOnce 只要发现 HTTPS 路径曾加载过就拒绝第一次 HTTP 回跳；key 又丢弃全部查询参数，把不同 service/execution 当成同一次请求。

- 第一次升级同一认证请求允许继续，仅同一请求再次升级才触发循环保护。保留 service、execution、renew 等实际参数，只忽略票据、nonce、timestamp 与缓存扰动参数；不同大厅服务回跳不再混为一谈。
- HTTPS 认证页内，同一认证站点的 HTTP 表单 action 转为 HTTPS，保留完整回跳及表单参数。页面可见和加载完成时均准备表单，原生与学校页面验证入口一起适配。跨站、非默认端口、非 POST 或不允许的嵌套 service 仍拒绝。
- 支持 CAS 登录 Servlet 的合法 ;jsessionid 路径；不放宽站点、协议和其他路径限制。直接 HTTP 密码 POST 仍阻断，错误提示与实际重复导航分开。

验证：117 项模拟表单检查、64 项导航检查、36 项 URL 策略检查通过，包含先访问 HTTPS 再遇到首次 HTTP 回跳、不同 service/execution、实际重复升级、HTTP 表单安全转为 HTTPS及站外回跳拦截。尚未连接 Android 真机，不能把代码回归检查等同于真实账号登录成功。

## 1.6.2：原生统一认证提交等待修复

- 原生登录不再等待 WebView 的 onPageFinished。DOM 解析结束后直接检查学校表单，因此图片或其他资源未加载完成不会一直阻挡填写与提交。
- 点击登录立即收起键盘，按钮显示连接、准备表单、提交和验证阶段。脚本异常与无效回调返回明确错误，失败弹出提示并滚动到操作区域。
- 普通重试重新加载认证页，保留认证会话，不再等待清空 Cookie 的串行回调；主动切换账号和清除凭证仍执行独立会话清理。
- 使用学校自身的表单提交处理；观察 submit 事件，检查实际 passwordEncrypt 字段是否已由学校处理，尚未加密时阻止提交并等待，避免发送空或明文替代的密码字段。网页按钮未触发表单提交或被学校校验阻止时明确提示。
- 包含 1.6.1 的重复跳转保护、登录状态识别、重复成功回调保护和报修移动版切换，以及上一轮最终构建中断后未进入旧 APK 的重试修复。

验证：95 项合成凭证表单检查覆盖 DOM interactive 而非 load 完成、未触发提交、提交被取消、缺少加密结果、明文结果拦截和字段隔离。大厅识别、URL 策略和导航检查沿用 1.6.1。没有连接的 Android 测试设备；尚未完成手机原生输入到学校认证成功的端到端验证。

## 1.6.1：统一认证跳转与报修移动版修复

- 按已连接的 Edge 实际页面适配：认证表单使用可见但无 name 的 password 输入框，保留学校隐藏的 passwordEncrypt 字段和自己的提交处理；注入时发送 input、keyup、change、blur 事件。
- 大厅登录状态改为识别 ampHasLoginTool、ampHeaderToolUser 与 DIV 类型的“安全退出”控件，不再只找链接或按钮形式的退出入口。已在用户打开的实际服务大厅验证识别结果。
- 同一次跳转过程只处理一次认证成功，避免重复保存后再次导航；进入认证模块保留已有会话，主动切换账号时才清理统一认证会话。
- HTTP 认证跳转最多升级一次；同路径重复跳转（忽略时间戳和票据参数）及总跳转次数均有上限。失败状态保持停止，加载超过 45 秒显示重试提示；仅用户主动重试开始新一轮。
- 报修入口先访问大厅实际授权入口 appShow?appId=5341551514791165，落到后勤电脑版并完成加载后切换用户提供的移动版地址。在 Edge 中用户完成认证后，电脑版页面空白；同一会话打开移动版后已显示报修区域、项目、地址、故障描述、联系人及预约表单。未提交任何报修。

验证：75 项模拟表单检查（凭证为合成数据）、9 项大厅状态检查、32 项 URL 策略和 52 项导航/重复回调检查通过。实际 Edge DOM 和移动版表单已验证；尚未完成 Android 真机原生输入、验证码及 WebView 登录的端到端验收，桌面浏览器成功不等于手机端已验收。

## 1.6.0：统一身份认证与网上报修

- 用户页新增独立的统一身份认证模块：原生账号/密码表单、自动登录开关、最近认证记录和清除登录入口。教务与统一认证使用不同的 Android Keystore 别名、加密文件和设置文件。统一认证文件为 noBackupFilesDir/identity-login.enc，不包含在备份或安装包中。
- 首页新增主题色“网上报修”入口，内置打开用户提供的 http://hqfw.jhun.edu.cn/wsbx/html/yd/wsbx.html#/wybx，沿用手机内的校园服务会话。跳转到学校认证页后，可使用已保存的统一认证凭证；失败后停止自动重试，交由用户处理。
- 原生表单调用学校自己的登录按钮处理请求，保留学校的表单令牌与密码处理逻辑，不模拟或绕过验证码。普通验证码可在原生表单输入；额外验证可切换到学校页面。学校页面中手动输入的密码不会被应用读取，因此该方式只建立会话，不保存新凭证。
- 报修网页支持通过系统图片选择器附加图片；表单内容和提交均由用户操作。加载、连接失败、网页提示和重试入口使用应用主题。
- 修复教务登录/退出原先清空所有 Cookie 的行为。现在只清理对应域名及路径的会话与网页存储，分别管理教务与统一认证。凭证清除保留课表、成绩、自定义课程和背景。
- 仅服务大厅和后勤站点两个明确的域名允许 HTTP；统一认证密码只注入 HTTPS 的 authserver.jhun.edu.cn/authserver/login，并校验表单目标以及嵌套 service 回跳地址。认证页的 HTTP 跳转升级到 HTTPS，证书错误不会忽略。

验证：65 项模拟登录 DOM/凭证注入边界检查、32 项认证 URL 策略检查、202 项会话清理范围模拟检查通过；原有教务登录适配与 50 项课表/成绩查询解析检查通过。资源/Java/Dex 和 APK 签名检查通过，与 1.5.7 使用同一包名和签名，可覆盖安装。

1.6.0 发布时的接入限制（1.6.1 已补充实际浏览器验证）：当时工具无法连接用户的 Edge；直接读取统一认证页面被浏览工具安全策略拦截，服务大厅返回 403。实现依据用户提供的完整地址及通用表单结构，尚未用真实账号验证学校当前 DOM、CAS 跳转、验证码和报修页面。模拟检查不代表真实网站端到端登录成功，不能承诺报修站点没有额外授权步骤。

## 1.5.7：更新应用包名

应用 ID、源码命名空间、测试包和本地构建脚本统一改为 `cn.jhun.sanjiaohu`，源码目录为 `app/src/main/java/cn/jhun/sanjiaohu`。版本号 22，版本名 1.5.7。

此版在 Android 上是独立应用，不能覆盖包名为 `cn.jhun.jiangke` 的 1.5.6 及更早版本。旧应用仍可保留，旧缓存、登录凭证、自定义课程和外观设置不会自动迁移；新应用需要重新登录与设置。保留现有签名证书。

验证：APK 编译和 v2/v3 签名通过，安装包实际包名和启动 Activity 均为新命名空间；8 组现有 Java 回归测试在新包名下通过。尚未进行真机安装验收。

## 1.5.6：内置校园地图

首页“校园地图”改为打开应用内独立 WebView，直接加载 https://gis.jhun.edu.cn/m/。顶部返回、刷新按钮和加载进度沿用当前主题；返回先退回地图内上一页，首页时退出地图。网络、HTTP 或安全连接失败显示主题色重试页，地图页面进程中断后也可重新加载。地图启用 JavaScript 和 DOM 存储，不接入教务登录桥，不启动外部浏览器，仅允许地图域名的 HTTPS 导航。

资源/Java/Dex 编译、APK 签名及与 1.5.5 的证书一致性检查通过。尚未完成安卓真机地图加载、缩放和返回交互验收。

## 1.5.5：校园地图与本地校历

首页新增第二排主题色入口：“校园地图”用系统浏览器打开 https://gis.jhun.edu.cn/m/；“校历”进入独立的原生图片查看页。校历为用户提供的江汉大学 2026—2027 学年原始 JPG，保存在 app/src/main/assets/calendar-2026-2027.jpg，随安装包分发，离线直接读取，不需要相册或存储权限。

原图字迹横向旋转，查看器默认逆时针旋转 90° 正向展示，初始完整适应屏幕。支持双指缩放、拖动、双击放大/复原，以及缩小、放大、适应和旋转按钮。所有变换只用于屏幕显示，原图未裁切或重新压缩。

验证包含缩放边界与手势焦点的纯 Java 检查、原图与 APK 内资源逐字节核对、APK 编译和新旧签名一致性检查。尚未进行安卓真机的手势和浏览器跳转验收。

## 1.5.4：新头像图标

替换为用户提供的白发蓝色狐耳头像。使用内置图像编辑处理外侧白边、延展蓝色背景和调整构图，原附件保存在 design/icon-reference.png，提示词与处理说明见 design/icon-edit-notes.md。图稿不预设圆角，交给桌面系统遮罩。自适应图标前景 18.5% 留边，在常规可视区域内显示图稿的 94.5% 尺寸，避免沿用旧版 4% 放大参数切掉头像。

已检查耳尖与脸部关键位置在圆形遮罩内的几何范围，并生成圆形、圆角方形和连续圆角的 HTML 预览。资源/Java/Dex 编译、APK 签名及与旧版证书匹配检查通过。尚未进行各品牌安卓桌面图标的真机验收。本次不更改应用名称、课程或成绩功能。

## 1.5.3：调色盘

主题调色的三个数值滑块改为圆形调色盘：点击或拖动选择色相和饱和度，下方渐变色带调明暗，并提供六个快捷色。保留六位颜色代码输入、跟随背景主色开关，以及主题色、今日标记和入口底色预览；色盘与代码同步，所有操作编辑草稿，点击保存才应用，取消保留原设置。

色盘拖动时阻止外层滚动抢占手势；键盘方向键也能调整，辅助功能用户可通过颜色代码精确输入。编译、签名和新旧证书匹配检查通过；尚未进行安卓真机拖动和外观验收。

## 1.5.2：周次校准与透明文字

- 周次校准改为统一主题底部面板：年月切换、月历选周、整周浅色标记和周一主色标记，上方即时显示第 1 教学周的周一日期。点击年月可快速选年/月，取消或关闭不写入，保存后更新当前学期周次。
- 移除透明文字衬底和描边，不绘制文字背后的矩形。根据课程所在位置的背景图采样及课程自身透明度选择中性色文字，仅使用轻微文字阴影辅助阅读。课程底色仍由透明度设置控制；100% 时没有课程卡片填充或文字衬底。
- 透明度预览与实际课表使用相同逻辑，图片采样与背景实际居中裁切对应。字体对比度检查针对采样后的背景色，不承诺复杂照片每个像素都达到相同对比度。

验证：资源/Java/Dex 编译、APK v2/v3 签名及新旧签名匹配检查通过。6,060 组采样背景文字颜色检查通过。未进行安卓真机的月历交互、背景采样效果与界面验收。

## 1.5.1：学期课表、有效成绩与汇总

- 点击课程页的学期名称，或「更多 → 切换学期」，读取学校提供的学期列表并查询所选学期课表。每个学期独立缓存，先显示对应缓存再更新；首次没有缓存时不会借用另一个学期的课程。旧的真实课表迁入对应文件，较新的缓存不会被旧文件覆盖。自定义课程保持独立。
- 点击「第 N 周」使用主题色的圆角底部面板和四列周次按钮；保留回到本周。星期与时间栏使用浅主题底色。
- 首页新增成绩查看。每学期读取学校的主修有效成绩，展示课程、学分、学时、分数、绩点和备注。分数下方直接显示学校提供的绩点，不自行换算。旧版原始成绩缓存仍可显示，刷新后升级为有效成绩。
- 成绩页面采用小型刷新图标，左侧为「汇总」入口。汇总默认展示入学以来，也可切换当前学期；从学校统计表读取各环节获得学分、平均成绩，以及合计平均成绩、平均学分绩点、加权平均成绩和获得学分，不将有重叠的分类相加。
- 成绩、学期课表和入学以来汇总分别以 AtomicFile 保存。离线、登录过期、表格缺行、分页不完整和学期不一致均不覆盖缓存。完全加载且经过查询范围验证的空结果可保存；空汇总不显示伪造的零平均分。多页报告在所有页均已加载时统一解析。
- 课程文字移除白色描边。普通深色字体配一块随透明度渐变的浅色衬底，仅覆盖文字区域，保持背景可见；预览和课表同步使用此效果。
- 第 4、8 节后增加午休、晚餐分隔；使用当前课表的结束与开始时间标记。左侧节次依次显示节次、上课时间、下课时间。七天十二节仍按可用屏幕分配空间，跨用餐时间的自定义课程分段展示，避免盖住分隔。
- 狐狸图标原图不重绘，自适应前景留边由 14% 改为 4%，放大主体、减少白边。包名、签名和本地数据目录保留，版本号为 16 / 1.5.1。

验证：在用户已登录的教务页面核对了历史学期切换、学期有效成绩、入学以来有效成绩、绩点列与汇总统计字段。50 项合成数据的解析/查询测试、10 项学期规范化、13,152 项用餐分隔布局、6,060 项透明文字对比度，以及原有课程/布局/主题/自定义课程回归检查通过。APK 资源、Java、Dex 编译与 v2/v3 签名核验通过，与旧版证书一致。未连接安卓设备或配置模拟器，因此尚未完成 Android WebView 真实账号端到端同步、离线恢复、桌面图标遮罩和真机界面验收；浏览器核对和尺寸/解析测试不等于真机测试。

## 1.4.6：三角狐名称与图标

应用名称、主页标题、登录页与更多菜单名称统一改为「三角狐」。图标替换为用户提供的水彩狐狸原图，图片内容未重绘，资源文件与原附件哈希一致。通过自适应图标 XML 的 14% 留边和暖白底适配桌面遮罩。

保留应用包名、签名及数据目录，可直接覆盖安装并保留课表、账号设置和背景。资源/Java/Dex 编译、APK v2/v3 签名和安装包应用名称核验通过；未进行安卓真机桌面图标验收。

## 1.4.5：退出登录与课程详情界面

- 用户页「退出登录并清除凭证」采用主题浅底、深色主题文字、细边框和按压反馈；处理中降低透明度并禁用。
- 退出登录确认改为圆角底部面板，明确说明关闭自动登录、保留本地课程和背景，仅确认后清除凭证。
- 点击课程后的详情改为统一底部面板：课程名称、时间卡片、地点、教师、教学周和学期分组展示。教务课程提供「知道了」；自定义课程提供同风格的「删除课程 / 编辑课程」。
- 删除确认沿用主题底部面板，取消不会修改课程；删除保存失败时仍保留确认面板与原记录。详情内容可滚动查看长文本。
- 公共面板的关闭、取消和确认按钮共享 themedButton 配色与按压样式，跟随背景自动主题或手动主题色。

验证：资源、Java、Dex 编译和 APK v2/v3 签名通过；与 1.4.4 同签名。1,007 组配色检查包含次要按钮文字与浅底的对比度。尚未进行安卓真机界面与交互验收。

## 1.4.4：背景配色刷新

- 首页入口底色直接从当前主题主色生成，保留更明显的色相，接近白色时适当降低亮度以区分页面背景；标题及说明维持可读对比度。
- 成功导入新背景后立即清除手动主题设置，恢复跟随背景主色并刷新全局界面。图片导入失败或取消选图时不修改原设置。重置为默认背景也恢复默认 #2ECBFF 主题。
- 手动调色仍可使用，保存后保持有效，直到下一次更换背景或恢复默认背景。

验证：1,007 组主题配色检查、APK 资源/Java/Dex 编译及 v2/v3 签名校验通过；与 1.4.3 同签名，可覆盖安装。尚未进行安卓真机换图与外观验收。

## 1.4.3：首页底色与手动主题调色

- 首页移除重复的查看课表、教务账号入口，保留半宽的自定义课程入口。入口使用更明显的主题浅色底；即使主色接近白色，也能与页面底色区分。
- 课程页「更多 → 外观 → 主题调色」新增统一底部面板：色相、饱和度、明亮度滑块、6 位十六进制颜色输入，以及主色、今日标记和入口底色的预览。
- 初始模式跟随背景主色（无背景时为 #2ECBFF），手动调整后保存为本机主题色。重新打开面板保留当前设置；开启「跟随背景主色」并保存即可恢复自动取色。取消、关闭或点外部不改动设置。
- 手动模式下，更换或恢复默认背景保留手动颜色，提示文案明确说明。主题色用于按钮、开关、今日标记及入口等全局控件，不改变用于区分课程的多色卡片。

验证：资源、Java、Dex 构建与 APK v2/v3 签名校验通过，包名和签名沿用 1.4.2。1,007 组主题检查覆盖入口底色区别与文字对比度。尚未进行安卓真机调色交互与外观验收。

## 1.4.2：统一主题、首页入口和切页过渡

- 用户页「自动登录」和登录页「保存凭证并自动登录」统一设置开关滑块及轨道配色，选中和禁用状态均从当前主题生成。
- 更多按钮仅在课程页面显示；菜单删去「账号 / 用户与登录」，保留课表和外观操作。
- 首页改为两列的适中入口卡片：自定义课程、查看课表、教务账号。自定义课程列表移入独立底部面板，继续支持添加、编辑、删除，首页不再呈现完整课程列表或专属大块空状态。
- 今日标记采用背景主色生成的深色，白色日期/今日文字的对比度至少为 7:1，不再固定使用深蓝灰。
- 页面切换加入 200ms 淡入与 10dp 横向位移，导航选中背景、图标和文字颜色同步渐变。重复点选当前页面不会重新渲染；快速切换、刷新和切到后台会取消旧动画；遵循系统关闭动画的设置。

验证：资源 / Java / Dex 编译、APK v2/v3 签名检查通过。1,007 组主题检查新增深色标记文字对比度，以及不同主色生成不同深色的验证；布局、课程及透明度回归检查通过。签名沿用 1.4.1，可覆盖安装。尚无连接安卓设备，未实测真机切页动画与开关外观。

## 1.4.1：导航对齐、课程透明度与表单设计

- 修复底部导航文字：文字控件明确使用居中 gravity / textAlignment，去掉额外字体内边距，三个栏目使用相同的图标和文字尺寸。之前栏目容器虽居中，填满栏目宽度的文字本身仍按左侧对齐。
- 「更多 → 外观 → 课程透明度」新增 0–100% 滑块、示例卡片预览、恢复不透明、保存和取消。默认 0%（完全不透明），仅改变卡片底色，不降低文字透明度；透底时用细白色字边保持中性深色字可辨。取消/返回/点击外部会恢复原设置；保存后重启仍生效。
- 首页移除大幅介绍卡及整行添加按钮，改为「我的课程」标题旁 44dp 高的小型「＋ 添加」入口；空状态使用轻量说明。
- 自定义课程使用统一圆角底部表单，浅色填充输入框、聚焦描边、星期快捷选择、起止节次加减、全部/单/双周快捷填充和学期选择。固定底部保存区；内容在小屏或键盘出现时滚动，入口与弹层均跟随主题配色。
- UiSheet 为后续编辑/设置界面提供统一圆角、间距、标题、关闭、按钮与轻量动效；新功能继续复用此视觉规范。

参考了用户提供的 [梨课程](https://course.njust.store/) 可见页面样式；该站主界面被使用协议遮挡，未代用户接受协议，也未声称完整复刻该站。

验证：Java/资源/Dex 编译通过；20 项解析、12,544 项布局及缓存、1,007 组主题、自定义课程和透明度 60 项检查通过。APK v2/v3 签名校验通过，与 1.4.0 签名相同。没有连接的 Android 设备，尚未完成真机界面、键盘避让、文字描边和透明度交互验收。

## 1.4.0：三个页面、自定义课程和自动登录

- 底部导航为「首页 / 课程 / 用户」。删除今日课表列表；课程页继续一屏显示七天十二节。首页提供自定义课程入口与管理列表，用户页显示已登录、正在验证、需要验证码或未登录等状态。
- 课程采用 12 种区分明显的蓝、橙、紫、绿、粉等颜色。同名课程颜色固定保存在设置中；卡片不透明，中性文字对比度不低于 4.5:1。背景图片与全局主题跟随主色功能保留。
- 今天的星期栏深色突出，默认显示日期，点击在「今日」和日期间切换。未校准时日期暂按当前自然周显示，教学周仍标记「待校准」；请用右上角菜单校准第一教学周的周一。
- 自定义课程支持名称、地点、教师、星期、起止节次、范围/单双周以及适用学期，支持编辑、删除。未登录也能添加。「所有学期」课程跨学期显示，其余仅在对应学期显示；切换学期不会删除课程。自定义课程单独写入 custom-courses.json，网络只更新 schedule.json；两者在显示时组合，时间冲突并排显示。
- 登录时可开启「保存凭证并自动登录」。账号密码使用 Android Keystore 中的 AES-256 密钥和 GCM 加密，密文存于应用 noBackup 私有目录，不保存明文密码。密码仅在成功验证后保存；加解密在后台线程进行。参照 [Android Keystore 文档](https://developer.android.com/privacy-and-security/keystore)。
- 每次回到前台优先复用会话，过期后自动尝试登录一次并刷新课表。验证码或明确验证失败会暂停自动尝试，提示用户手动登录；网络失败保留缓存，不无限提交密码。用户页可关闭自动登录并清除凭证，或退出并清除会话；本地课程继续保留。
- **从旧版升级后，请手动登录一次并开启保存凭证**，旧版没有保存过密码。手动登录会先清理旧会话，避免更换账号时直接被旧会话跳回课表。

验证：课程解析 20 项、布局与升级缓存 12,544 项、主题 1,007 组、自定义课程组合/筛选/校验与配色 55 项通过；两组 Node 测试验证登录提交、特殊字符、来源限制、会话识别及验证码状态。资源/Java/Dex 构建和 APK v2/v3 签名校验通过；包名和签名沿用旧版。当前无连接 Android 设备或配置的模拟器，**尚未实测真机界面、Keystore 保存读取、真实账号自动续登及完整断网恢复流程**。

## 1.3.2：更多菜单外观

右上角更多按钮打开自定义主题面板：22dp 圆角、细边框、柔和阴影、线条图标、跟随背景主色的浅色底与中性文字，替代系统默认 PopupMenu。分为「课表 / 外观 / 账号」三组，六个原有入口保持对应功能。点击菜单外、右上角关闭按钮或系统返回可关闭。

150ms 淡入缩放、100ms 淡出。面板随可用屏幕尺寸限制高度，内容较多或大字体时可滚动。后台切换或主页重新渲染时主动关闭，避免残留浮层。

资源与 Java 编译通过，APK v2/v3 签名验证通过。未进行本轮安卓真机视觉验收。

## 1.3.1：名称与文字样式

应用显示名称、主页标题和登录页标题改为「三角湖边」。保留原应用包名及签名以支持覆盖安装，保留已同步的课表、登录状态和背景图片。

取消主色反色文字，改用中性深灰文字；深色按钮上的文字自动调整为清晰的浅灰/白色。星期、节次与时间栏去掉逐格的主色底块，恢复轻量文字样式。使用自定义背景时，仅在顶部和左侧使用连续浅底，保证文字不会与图片混在一起。背景选择和提取图片主色功能保留。

1007 组配色的文字对比度与中性色检查通过；APK 编译、签名校验通过，与 1.3 同签名，安装包名称核验为「三角湖边」。本轮未做安卓真机外观检查。

## 1.3：原生登录、自定义背景和图片配色（历史记录）

- 登录默认只显示原生账号、密码框和登录按钮，不展示学校网页。应用后台使用学校原页面执行登录，保留学校已有的密码处理及登录会话。账号密码不会写入应用设置、日志或课表缓存。
- 已确认真实登录入口为 https://jwxt.jhun.edu.cn/cas/login.action，账号、密码、登录按钮分别为 username / password / login；正常登录免验证码。若学校要求图片验证码，才展开原生验证码输入和图片，需用户自己填写。其他额外认证流程尚未适配。
- 右上角 ⋮ →「更换课表背景」：系统选图器选择图片，应用导入一份到私有目录，之后无需访问原相册图片。支持居中裁切、按原比例显示和 EXIF 方向校正；大图片下采样到最长边不超过 1600 像素。
- 自动统计图片主色，界面背景、按钮、文字、课程卡片同步换色。课表文字从主色反色出发调整明暗，课程卡片与文字对比度至少 4.5:1。课程卡片保持不透明，以免背景花纹影响阅读。
- 「恢复默认背景」清除导入图片，恢复 #2ECBFF 主色。不删除课程缓存。

验证：真实网站只读检查确认字段、密码类型及表单来源符合适配条件；Node 模拟测试覆盖提交、特殊字符、来源限制和可选验证码；Java 测试覆盖 1007 组主色与文字对比度。APK 编译、签名通过，与 1.2 同签名，可直接覆盖安装。未使用真实账号提交登录，未完成安卓设备选图、恢复默认、重启或界面测试；不能将适配检查等同于真实登录通过。

新增测试：

~~~powershell
node tests/login.test.cjs
javac -encoding UTF-8 -d test-classes app/src/main/java/cn/jhun/sanjiaohu/ThemePalette.java tests/ThemeTest.java
java -cp test-classes cn.jhun.sanjiaohu.ThemeTest
~~~

## 1.2：图标与主题

采用用户提供的原始图片作为应用图标，未重绘或修改图片内容。通过 Android 自适应图标的前景留边与系统遮罩适配圆形、圆角方形等桌面形状。参照 [Android 自适应图标规范](https://developer.android.com/develop/ui/views/launch/icon_design_adaptive)，把图案放在安全范围内，避免外圈文字被裁切。

主色为 #2ECBFF，选中标签、登录主按钮及系统强调色采用此颜色；底色、课表卡片和辅助颜色统一为浅蓝色系，按钮使用深色文字以保证可读性。图标保留原图的颜色。

已通过资源编译、APK 签名校验，图标源文件与用户提供文件的 SHA-256 一致，签名与 1.1.1 一致。可直接覆盖安装，保留已同步课表。未做本轮真机外观检查。

## 1.1.1：课表区域空白修复

针对“已有学期与周次，但课表区域空白”的反馈，将课表改为独立 WeekGridView：在渲染时先创建全部星期、节次和课程子视图，再分别在 onMeasure、onLayout 中完成测量及摆放。移除在 OnLayoutChangeListener 中动态添加格子的方式。继续保留一屏七天十二节和右上角菜单。

不更改已保存的真实课程或登录状态。与 1.1 同签名，可直接覆盖安装，无需卸载。

60 种可用尺寸下，实际课程矩形与布局边界检查通过；纯 Java 检查共 12,544 项，原课程解析检查 20 项通过。编译和 APK 签名校验通过。新增 tests/android/GridInstrumentation.java 用于真实 Android 首次 measure/layout 检查，但当前无连接设备，**未执行该设备测试，也未验证真机画面**。

## 1.1 的修改

1. **登录后才获取课程。** 安装包不再包含个人课表，也没有首次启动导入逻辑。首次打开显示登录引导。
2. **迁移旧版本。** 覆盖安装时会识别并清除 1.0 自动导入的快照；如果用户已真正登录并成功同步，保留那份本地缓存。
3. **重做登录页。** 独立 Activity 和完整尺寸的 WebView，不再把用于后台抓取的透明 1×1 WebView 移入弹窗。入口改为学校网站根地址，提供加载进度、超时、网络/HTTP/证书错误、重新加载和电脑版切换。成功进入教务首页会返回并同步，也可以点击「完成登录」。
4. **周课表一屏显示。** 根据实际可用宽高分配七天与十二节课，去掉周课表的横向、纵向滚动容器，课程文字自动缩放。长名称和极端小屏上的文字可能较小，点按课程可看完整详情。今日列表仍可滚动。
5. **右上角 ⋮ 菜单。** 更新课表、回到本周、周次校准和教务登录集中在菜单里，移除原底部功能按钮。

## 安装与使用

将 Sanjiaohu-1.6.0.apk 传到手机安装，可覆盖 1.5.7。与 1.5.6 及更早版本的包名不同，不能覆盖那些版本，也不会自动继承其数据。

首次打开可在首页添加自定义课程；用户页打开原生账号密码登录表单，登录后获取并保存教务课表。右上角 ⋮ →「周次校准」选择第一教学周的周一日期，以准确映射教学周与实际日期。

之后每次打开或返回前台，先展示缓存，再后台更新当前数据页面；打开成绩或汇总也会自动尝试更新。网络、登录或解析失败时保留该范围的上次记录。尚未登录、也没有已同步缓存时，只显示用户自己添加的自定义课程，不预装个人数据。

## 登录白屏的排查范围

旧版直接打开登录后的首页、复用隐藏的小尺寸 WebView，而且错误提示位于被弹窗挡住的主页。1.1 针对这些路径作了修改。没有手机日志，因此不能把其中某一项确定为此次白屏的唯一根因。

本次独立访问学校登录入口也发生超时，手机端的实际登录仍待验证。学校官方公布的入口为 [教务系统首页](https://jwxt.jhun.edu.cn/)，可参见 [学校选课通知](https://jwzx.jhun.edu.cn/83/5f/c7525a230239/pagem.htm)。若 1.1 仍无法登录，页面会显示失败原因，可提供这段错误文字以继续定位。

## 数据与隐私

- 不预装课表、不复制电脑 Cookie、不记录明文密码。用户开启自动登录后，仅保存本机加密凭证。
- WebView 登录会话留在本机；课表、成绩、汇总 JSON 保存在应用私有目录，关闭系统云备份。汇总仅保存聚合指标，不额外保存整份入学以来成绩明细。
- 教务和地图仅允许学校 HTTPS 域名导航。统一认证使用 HTTPS，用户提供的服务大厅、后勤报修 HTTP 地址通过精确域名配置开放；不跳过证书验证。
- 缓存完整校验后通过 AtomicFile 原子替换；未验证范围的空结果或不完整响应不覆盖缓存。
- 新同步的缓存带有 source: school-sync 来源标记。
- 按学期保存 schedule-年-学期.json 与 grades-年-学期.json，入学以来汇总存于 summary-all.json；网络同步不修改 custom-courses.json。换账号时，在新账号成功同步前仍保留旧缓存。

## 验证

- 原课程解析的 20 项 Java 检查继续通过。
- 仓库内的 ThemeTest 仍是单模式 1007 组，对本版新配色继续通过：正文与次要文字对比度、按钮与面板对比度、入口底色区分、课程卡片可读度。
- 深色配色另在开发机上用扩展套件验证过：1007 浅 + 1007 深共 2014 组，额外断言错误色对比度、深浅两套文字在今日标记上的可读性、深浅模式必须产生不同底色与卡片，以及 overlay() 的 alpha 合成与全透明恒等。该扩展套件按「仓库仅收录应用代码」的约定未提交，因此**仓库内的 CI 不覆盖深色配色**。其中「语义角色必须区别于所在页面」一条只对 8 个真实主题色断言：当主题色接近纯白时，所有「向白混合」的角色都会与页面塌成同值，entrySurface 早有针对同一情形的对比度保护，故该不变量不能对全部随机主色成立。
- 深色模式仅经代码层与配色断言验证，**未在真机或模拟器上目视确认**：本机既无已连接设备也无已配置模拟器。开关的持久化、切换后的重绘、状态栏图标极性以及壁纸遮罩均未经过实际渲染检查。
- tests/ 下另有 4 个套件此前未接入 CI，现已补入 workflow：IdentityPolicyTest（47 项）、IdentityNavigationTest（59 项）、IdentityDiagnosticsTest（31 项）、SessionCookiesTest（202 项），合计 339 项断言，覆盖统一认证 URL 策略、导航防循环与诊断脱敏。
- GitHub Actions（.github/workflows/gradle.yml）已在 runner 上完成三次构建，全部 success。其中 main 的 33850f2 与 PR 的 9c2f6fc 两次包含新增的两个测试步骤，步骤级结论为 success：workflow 使用 set -euo pipefail，断言失败会令步骤非零退出，因此可确认「Run identity regression tests」与「Run session cookie tests」确实执行并通过，而非被跳过。CI 用 javac/java 直接运行 tests/ 下的套件，因为该套件未接入 Gradle 的 test source set；项目不带 Gradle Wrapper，workflow 改用 gradle/actions/setup-gradle 提供 Gradle 8.13。
- RevisionTest 验证旧快照迁移、真实缓存保留和 60 种可用视口尺寸下的七列十二行边界。
- APK 版本号 33 / 版本名 y7c_0.2，包名 cn.jhun.sanjiaohu。1.0.1 公共发布版沿用 1.5.7 签名，可覆盖 1.5.7；本机用 build-local.ps1 在全新 -Work 目录构建的包会生成新密钥，无法覆盖安装上述版本，强行安装需先卸载，并会清除课表、成绩、自定义课程、背景与登录凭证。
- 校历视口 116 项检查通过，覆盖横竖屏完整适应、四向旋转、缩放焦点和拖动边界；APK 内校历 JPG 与用户附件逐字节一致。
- APK 不含 seed.json、学生成绩、账号或浏览器 Cookie，解析测试仅使用虚构数据。
- 当前没有已连接安卓设备或已配置模拟器，尚未完成真机界面、登录和断网恢复测试。尺寸边界测试不等同于真机截图检查。

## 构建

可在 Android Studio 打开项目，或用现有 Android SDK 运行：

```powershell
./build-local.ps1 -Sdk 'Android SDK 路径' -Java 'JDK 路径' -Platform 'android-36.1' -BuildTools '36.1.0'
```

Gradle 配置使用 AGP 8.13.0、compileSdk 36、minSdk 26、targetSdk 35。不附 Gradle Wrapper 二进制。直接构建不需要下载 Gradle 依赖。

签名密钥是 build-local.ps1 在 -Work 目录内按需生成的 development.keystore，不放在源码包内。复用同一个 -Work 目录才能保留密钥，后续版本才能覆盖安装；换用新的 -Work 目录会生成新密钥。

```powershell
javac -encoding UTF-8 -d test-classes app/src/main/java/cn/jhun/sanjiaohu/Course.java app/src/main/java/cn/jhun/sanjiaohu/CachePolicy.java app/src/main/java/cn/jhun/sanjiaohu/GridGeometry.java tests/CourseTest.java tests/RevisionTest.java
java -cp test-classes cn.jhun.sanjiaohu.CourseTest
java -cp test-classes cn.jhun.sanjiaohu.RevisionTest
```

文字缩放采用 Android [TextView 自动字号](https://developer.android.com/develop/ui/views/text-and-emoji/autosizing-textview)。

新增教务适配与布局检查（在项目目录运行）：

```powershell
node tests/portal.test.cjs
javac -encoding UTF-8 -d test-classes app/src/main/java/cn/jhun/sanjiaohu/Term.java app/src/main/java/cn/jhun/sanjiaohu/GridGeometry.java app/src/main/java/cn/jhun/sanjiaohu/CourseAppearance.java app/src/main/java/cn/jhun/sanjiaohu/CourseColors.java app/src/main/java/cn/jhun/sanjiaohu/ThemePalette.java tests/TermTest.java tests/MealGridTest.java tests/AdaptiveTextTest.java
java -cp test-classes cn.jhun.sanjiaohu.TermTest
java -cp test-classes cn.jhun.sanjiaohu.MealGridTest
java -cp test-classes cn.jhun.sanjiaohu.AdaptiveTextTest
```
